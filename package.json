{
  "name": "rustrsssrv-assets",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tailwindcss -i ./assets/css/input.css -o ./assets/css/tailadmin.css --minify",
    "watch": "tailwindcss -i ./assets/css/input.css -o ./assets/css/tailadmin.css --watch",
    "build:all": "npm run build && cargo build --release"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.16",
    "tailwindcss": "^3.4.17"
  },
  "dependencies": {
    "jquery": "^4.0.0"
  }
}
